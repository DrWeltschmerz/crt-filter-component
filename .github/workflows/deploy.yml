name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Prepare demo for deployment
        run: |
          cp -r assets demo/
          cp -r component demo/
          find demo -name "*.html" -type f -exec sed -i "s|'../assets/|'./assets/|g" {} +
          find demo -name "*.html" -type f -exec sed -i 's|"../assets/|"./assets/|g' {} +
          find demo -name "*.html" -type f -exec sed -i "s|'../component/|'./component/|g" {} +
          find demo -name "*.html" -type f -exec sed -i 's|"../component/|"./component/|g' {} +
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'demo'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
